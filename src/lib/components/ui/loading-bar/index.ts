import Root, {
	loadingBarVariants,
	type LoadingBarSize,
	type LoadingBarVariant,
} from "./loading-bar.svelte";

export {
	Root,
	type LoadingBarSize,
	type LoadingBarVariant,
	//
	Root as LoadingBar,
	loadingBarVariants,
};

