export function translateZodError(error: string): string {
	// This is a placeholder - actual translation happens in the Svelte component
	// The component will use $t() to translate the error message
	return error;
}
