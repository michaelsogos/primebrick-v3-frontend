---
description: GitFlow guardrails — branch before ANY edit; never commit on main/develop
alwaysApply: true
---

# GitFlow guardrails (frontend)

## Zero tolerance (agents — non-negotiable)

1. **Never work from `develop` or `main`.** No patches, writes, deletes, or **commits** on those branches—only `checkout` to refresh and **immediately** `checkout -b fix/<slug>` or `feature/<slug>`.
2. **Never push commits made on `develop`.** Commit on **`fix/*` / `feature/*`**, merge into `develop`, then push. *"Push and close"* = merge the task branch first, **not** commit on `develop`.
3. **Only explicit overrides:** e.g. *"commit directly on `develop`"*. Ordinary *"push"* requests do **not** waive branching.
4. If `HEAD` is `develop`/`main`, **branch before the first edit.**

## Mandatory order (agents): branch **before** any tracked-file edit

Applies to **every** change in **this** repo (source, docs, `.cursor`), including when the workspace meta repo is not open.

1. **Before** the first tool that **creates, modifies, or deletes** a tracked file, run: `git branch --show-current` (from this repo root).
2. If **`main` or `develop`**: `git fetch` (or `pull`) as needed, then **`git checkout -b fix/<slug>`** or **`feature/<slug>`**, **then** edit files.
3. If already on **`feature/*`**, **`fix/*`**, **`release/*`**, **`hotfix/*`**: proceed.
4. Documenting GitFlow in prose is **not** enough—you must **run** `git checkout -b …` when on `develop`/`main`.

Unless the user **explicitly** orders commits directly on `develop`/`main` (see **Zero tolerance**).

## Hard rules

- **Do not commit code or configuration changes on `main` or `develop`.**
  - If `HEAD` is `main` or `develop`, **create** `feature/*` or `fix/*` first (see **Mandatory order**), then edit—do not only “ask” and still patch files on `develop`.
- **All work must happen on a GitFlow branch**:
  - `feature/*` (from `develop`)
  - `release/*` (from `develop`, SemVer like `release/0.2.0`)
  - `hotfix/*` (from `main`, SemVer like `hotfix/0.2.1`)

## Required user prompts

### On `main` or `develop`

- Stop before editing. Ask which GitFlow branch type to use (`feature`, `release`, `hotfix`) and how to name it (or confirm your proposal).

### Already on `feature/*`, `release/*`, or `hotfix/*`

- **Ask** whether to **keep the current branch** for this work or **start on a new** GitFlow branch.
- If the user **stays** on the current branch: proceed **without** arguing about whether the branch name “matches” the task.
- If the user **opts for a new** branch (especially a new `feature/*`):
  1. **Ask** whether they want to **close the previous branch first** (merge per GitFlow + delete) or **leave it open**.
  2. **Always base the new branch on the correct GitFlow parent**: for `feature/*` and `release/*` use **`develop` updated** (`checkout develop` → `pull` → `checkout -b …`); for `hotfix/*` use **`main` updated** the same way. Do **not** branch the new feature off the old feature tip unless the user explicitly requests that.
- For a **new** branch name, you may propose a meaningful `feature/*` (etc.) name; if the user wants to choose the name, use theirs.

### After a completed chunk of work

- Ask whether to **close the branch** (merge per GitFlow + tag if release/hotfix) or **continue** on it.
- Do **not** push to remote automatically. Only push when the user explicitly asks.
- Do **not** commit changes until they have been verified to work (by running checks and/or visually confirming the fix).

## Closing branches (GitFlow hygiene)

When the user chooses to **close** a branch:

- Merge to the appropriate base branch (GitFlow), using `--no-ff`.
- For `release/*` and `hotfix/*`, create the tag derived from the branch name.
- After the merge, **delete the feature/release/hotfix branch** locally and on the remote.

## Communication style for git operations

- Keep git-operation updates extremely short (e.g. “Sto eseguendo un pull”, “Sto creando la branch”, “Sto facendo merge”). Avoid long explanations unless the user asks.

## Versioning rules (SemVer, major stays 0)

- Release branches bump **MINOR**: `v0.x.y` → `release/0.(x+1).0`, tag `v0.(x+1).0`.
- Hotfix branches bump **PATCH**: `v0.x.y` → `hotfix/0.x.(y+1)`, tag `v0.x.(y+1)`.
- Tag name must be derived from the branch name (`release/0.2.0` → tag `v0.2.0`).

