# Agent documentation (frontend)

Use this folder for **longer** human/agent reference material. Day-to-day rules for code under `src/` are in **`.cursor/rules/frontend-src.mdc`** (scoped by path — avoids loading full narrative on every chat).

| File | Contents |
|------|----------|
| `patterns.md` | Page layout, UI conventions, global **SheetHost** side panel, Shadcn vendor workflow, dev server etiquette |

Keep **`AGENTS.md`** at repo root as the short entry point.
